Cinchapi Exercise 3

Contents:

src: contains source file(s)
README
DAG.jar

To run:

$ java -jar DAG.jar

Expected output:

A series of true and false depending on whether the edge was added to the graph
Adjacency list representation of the final graph


To Test:

$ cd src
$ javac -cp .:junit-4.12.jar DAGTest.java 
$ java -cp .:junit-4.12.jar:hamcrest-core-1.3.jar org.junit.runner.JUnitCore DAGTest

